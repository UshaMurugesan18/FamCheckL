spring.application.name=FamilyChecklist
server.port=${PORT:8080}

# ── MySQL: parsed from MYSQL_URL by DataSourceConfig.java ─────────
# (MYSQL_URL is set in Railway via: ${{ MySQL.MYSQL_URL }})
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ── JPA ────────────────────────────────────────────────────────────
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ── SSE timeout ────────────────────────────────────────────────────
spring.mvc.async.request-timeout=-1

# ── VAPID keys for Web Push ────────────────────────────────────────
vapid.public.key=${VAPID_PUBLIC_KEY:BPqk3hp1RogRajnyOcwzAtKKM7vrpdVNW_sATZQoyEIJrbr1lcwcKkhaW-qDPEVE4hQuEArzfOuzYVF4EWvFbFU}
vapid.private.key=${VAPID_PRIVATE_KEY:qLLhoumuIpxNu2Q2f71iDopws7UX_2wT31XiAoOAY_Q}

